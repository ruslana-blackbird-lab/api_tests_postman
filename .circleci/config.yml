version: 2.1
orbs:
  newman: postman/newman@0.0.2
jobs:
  build:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: ./Get-Location-By-ID.postman_collection.json
          environment: ./Locations-environment.postman_environment.json